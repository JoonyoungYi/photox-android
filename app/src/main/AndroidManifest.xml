<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.photox.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Photox">

        <!--  -->
        <activity
            android:name="kr.photox.android.view.SplashActivity"
            android:theme="@style/Theme.Photox.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--  -->
        <activity
            android:name="kr.photox.android.view.LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.Photox.NoActionBar" />
        <activity
            android:name="com.facebook.LoginActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!--  -->
        <activity
            android:name="kr.photox.android.view.XActivity"
            android:icon="@drawable/ic_launcher_transparent"
            android:label="@string/app_name" />
        <activity
            android:name="kr.photox.android.view.PlaceActivity"
            android:label="@null"
            android:theme="@style/Theme.Photox.Dialog"></activity>
        <activity
            android:name="kr.photox.android.view.MissionActivity"
            android:icon="@drawable/ic_launcher_transparent"
            android:label="@string/title_activity_mission"></activity>

        <!-- Meta Data -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/fb_app_id" />

    </application>

</manifest>
